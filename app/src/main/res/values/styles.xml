<resources>

    <!-- Base application theme. -->
    <style name="Passport" parent="@style/Theme.AppCompat">
        <!-- customize the color palette -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:dialogTheme">@style/Theme.AppCompat.Light.Dialog</item>
    </style>

    <style name="Passport.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowActionModeOverlay">true</item>
    </style>

    <!-- Base application theme. -->
    <style name="Passport.Home" parent="@style/Passport.NoActionBar" />
    <style name="Passport.Register" parent="@style/Passport.NoActionBar" />
    <style name="Passport.Login" parent="@style/Passport.NoActionBar" />
    <style name="Passport.Main" parent="@style/Passport.NoActionBar" />

    <style name="Passport.Translucent" parent="@style/Passport.NoActionBar">
        <!-- using a semi-transparent window background for the drag-back gesture -->
        <item name="android:windowBackground">@color/scrim</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <style name="Passport.Translucent.AddCity">
        <item name="android:windowAnimationStyle">@null</item>
    </style>

</resources>
